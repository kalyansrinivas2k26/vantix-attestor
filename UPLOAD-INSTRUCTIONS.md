# Upload instructions

Upload every file in this delta to the existing `vantix-attestor` repository using the same relative path, replacing the older file where one exists.

Commit message: `Synchronize Attestor closure documentation and migration record`

After commit, wait for GitHub Actions. A green run is the final technical/documentation closure evidence. The demo remains the only intentionally pending presentation artifact.
