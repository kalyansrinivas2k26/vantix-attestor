# Evidence Provenance

## Evidence classes

### Owner-run synthetic n8n execution
The three module screenshots/reports support only the synthetic execution claims stated in the README:
- Commitment Assurance: 20 nodes green.
- Service Recovery: 20 nodes green.
- Customer Momentum: 24 nodes green.

### Owner-run synthetic adversarial n8n execution
`evidence/reports/adversarial-regression-v0.1.html` records **18/18 PASSED** and explicitly states that no live Salesforce, model-provider, or customer action occurred.

### Offline exact-node execution
`evidence/offline-exact-node-test-results.json` is generated by executing JavaScript directly from the shipped adversarial workflow JSON. It is labelled `OFFLINE_EXACT_NODE_CODE_EXECUTION` with `n8nRuntimeExecution=false`.

It is regression evidence, not a substitute for n8n runtime evidence.

### Repository-integrity evidence
`SHA256SUMS.txt` binds the final package file set. GitHub Actions is configured to re-run validation after upload. Historical CI success is not automatically transferred to a new commit.

## Prohibited evidence conversions

Do not convert synthetic n8n execution into production evidence; synthetic AI replay into live-model evidence; a labelled human-approval fixture into authenticated enterprise approval; task completion into customer-outcome proof; internal scoring into certification; or historical CI success into CI Green for a new commit.
